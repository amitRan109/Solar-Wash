const panelNumber = document.getElementById('panelsNumber');

panelNumber.addEventListener("change", function() {
    // const result = document.querySelector('.result');
    // result.textContent = `You like ${event.target.value}`;
    alert("chn")

    // var amountOfFaucets = Math.floor(event.target.value / 50);
    // if (event.target.value / 50 % 50 > 0) amountOfFaucets++;
    // document.getElementById('faucetsNumber').textContent = amountOfFaucets;
});